local randomNumber = math.random(1, 6) -- math.random(min value, max value) this generates a random number from min to max
print(randomNumber)

local baseplate = game.Workspace.Baseplate
local counter = 0
while counter < 25 do 
	local redValue = math.random(0, 255)
	local greenValue = math.random(0, 255)
	local blueValue = math.random(0, 255)
	
	baseplate.Color = Color3.fromRGB(redValue, greenValue, blueValue)
	task.wait(1)
	counter += 1
end

local randomValue = math.random(50, -20) -- this is not valid as the maximum value must be less than the minimum value
math.random() -- this is going to pick a random decimal number between 0 up to 1 by default
